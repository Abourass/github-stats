# Example environment variables
# Copy this file to .env and fill in your values

# Required: Your GitHub personal access token with repo and read:user permissions
ACCESS_TOKEN=ghp_your_token_here

# Required: Your GitHub username
GITHUB_ACTOR=your_username

# Optional: Comma-separated list of repositories to exclude (in owner/repo format)
# EXCLUDED=username/private-repo,username/test-repo

# Optional: Comma-separated list of languages to exclude
# EXCLUDED_LANGS=HTML,CSS

# Optional: Set to "true" to exclude forked repositories from statistics
# EXCLUDE_FORKED_REPOS=true
